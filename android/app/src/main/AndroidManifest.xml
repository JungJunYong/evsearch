<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EVSearch"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Kakao Map SDK Native App Key Placeholder -->
        <meta-data
            android:name="com.kakao.vectormap.APP_KEY"
            android:value="${KAKAO_APP_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.EVSearch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Glance AppWidget Receivers (4x1, 4x2, 4x3) -->
        <receiver
            android:name=".widget.ChargerWidget4x1Receiver"
            android:exported="true"
            android:label="@string/widget_name_4x1">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/charger_widget_4x1_info" />
        </receiver>

        <receiver
            android:name=".widget.ChargerWidget4x2Receiver"
            android:exported="true"
            android:label="@string/widget_name_4x2">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/charger_widget_4x2_info" />
        </receiver>

        <receiver
            android:name=".widget.ChargerWidget4x3Receiver"
            android:exported="true"
            android:label="@string/widget_name_4x3">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/charger_widget_4x3_info" />
        </receiver>

    </application>

</manifest>
